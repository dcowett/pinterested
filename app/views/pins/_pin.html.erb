<div id="<%= dom_id pin %>">

<p>
    <% if pin.image.attached? %>
      <%= image_tag pin.image.variant(resize_to_limit: [ 200, 200 ]) %>
      <% else %>
      <%= image_tag "default_image.png" %>
    <% end %>
    </br>
    <%= pin.description %>
    </br>
    <%= pin.user.name %>
  </p>

</div>
